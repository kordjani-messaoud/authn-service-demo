pipeline {  
    agent any
    stages {
        stage("build") {
            steps {
                echo "Building image"
            }
        }
        
        stage("push") {
            steps {
                echo "Pushing image to Harbor registry"
            }
        }
    }
}